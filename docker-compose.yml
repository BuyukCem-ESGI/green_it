version: "3.7"

services:
  backend:
    image: node:lts
    working_dir: /app
    env_file: .env
    container_name: backend
    volumes:
      - ./backend:/app
    ports:
      - '3000:3000'
    command: sh -c "npm i && npm run start:dev"
